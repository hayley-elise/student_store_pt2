const db = require("../db")


class Order {

    static async listOrdersForUser() {
        // return all orders that the authenticated user has created
    }

    static async createOrder() {
        // take a user's order & store it in db
    }

}


module.exports = Order